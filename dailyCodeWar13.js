/*Given an array of integers, return a new array with each value doubled.

For example:

[1, 2, 3] --> [2, 4, 6]*/


const maps = (x)=> x.map(x => x + x);

//psuedo code
//used the .map() method on the array to return a new array with the elements from inside of the array being doubled